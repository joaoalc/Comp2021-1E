class Fac {

    public int compFac(int num){
        int num_aux;
        boolean a;
        a = (25 * num) < 1 + 3 / 25;
        if(num < 1 + 3 / 25){
            num_aux = 1;
        }else{
            num_aux = 2;
        }
        return num_aux;
    }
}